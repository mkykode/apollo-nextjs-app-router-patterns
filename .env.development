# Loaded by `next dev` only (never by `next build` or `next start`); committed on purpose.
# Signs the demo session cookie in development. Set a real AUTH_SECRET in .env.local or in production.
AUTH_SECRET=development-only-secret-not-for-deployment
